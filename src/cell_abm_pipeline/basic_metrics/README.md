The `basic_metrics` package contains modules for calculating and plotting basic simulation metrics.

```bash
Usage: basic-metrics [OPTIONS] COMMAND [ARGS]...

Options:
  -w, --working TEXT
  --help              Show this message and exit.
```
