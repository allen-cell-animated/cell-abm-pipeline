The `resource_usage` package contains modules for quantifying resource usage (wall clock and object storage) for simulation sets.

```bash
Usage: resource-usage [OPTIONS] COMMAND [ARGS]...

Options:
  -w, --working TEXT
  --help              Show this message and exit.
```
